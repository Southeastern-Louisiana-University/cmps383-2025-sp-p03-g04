import { Colors, UIColors, TheaterMode } from "./colors";
import { Spacing, BorderRadius, Typography } from "./spacing";

export const Theme = {
  Colors,
  UIColors,
  TheaterMode,
  Spacing,
  BorderRadius,
  Typography,
};

export * from "./colors";
export * from "./spacing";
